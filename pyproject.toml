[project]
name = "sensor-analysis"
version = "0.1.0"
description = "Analysis tools for CO2 sensors"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "marimo[mcp,recommended]>=0.23.8",
    "polars>=1.41.0",
    "altair>=5.0.0",
    "ipython",
]
